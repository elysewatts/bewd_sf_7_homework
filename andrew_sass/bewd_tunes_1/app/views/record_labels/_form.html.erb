<div class="col-md-6">
	<h1 style="color:#cc3333;">Create New Record Label</h1>

	<div class="well">
	<%= simple_form_for(@record_label, html: {:class => 'form-horizontal'} ) do |f| %>

	<p><%= f.input :name, input_html: {:class => 'form-control', placeholder: 'Record Label Name'} %></p>

	<p><%= f.association :artist, input_html: {:class => 'form-control'} %> </p>

	<div style="text-align:center;">
	<p><%= f.submit "Submit".html_safe, :class => 'btn btn-lg btn-danger' %></p>
	</div>

	<% end %>
</div>
</div>